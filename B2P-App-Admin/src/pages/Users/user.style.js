import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles((theme) => ({
  itemBox: {
    background: '#FFE6D4',
    margin: '0.45rem 1rem',
    fontSize: '1.15rem',
    padding: '1rem 2rem'
  }
}))
