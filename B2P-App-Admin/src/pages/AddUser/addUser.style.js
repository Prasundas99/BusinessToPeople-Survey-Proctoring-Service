import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles((theme) => ({
  paper: {
    borderRadius: '1rem',
    margin: '2rem auto',
    padding: '3rem'
  },
  field: {
    margin: '1rem auto'
  },
  button: {
    borderRadius: '0.6rem',
    margin: '0.5rem auto'
  }
}))
